# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
mangum>=0.17.0  # ASGI adapter for serverless (Vercel, AWS Lambda, etc.)
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0  # For PostgreSQL
aiosqlite>=0.19.0  # For SQLite (default)
alembic>=1.12.0

# HTTP clients
requests>=2.31.0
httpx>=0.25.0

# Web scraping
beautifulsoup4>=4.12.0
lxml>=4.9.0
selenium>=4.15.0
webdriver-manager>=4.0.0
playwright>=1.40.0  # Modern browser automation for LinkedIn scraping

# Email verification
dnspython>=2.4.0
email-validator>=2.0.0

# Environment variables
python-dotenv>=1.0.0

# Utilities
python-multipart>=0.0.6
openpyxl>=3.1.0  # For Excel export (optional but recommended)

# Authentication
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0  # For JWT tokens (optional)

# Additional dependencies for enrichment
cryptography>=41.0.0  # For secure token generation

# AI/ML (optional - install as needed)
numpy>=1.24.0  # Required for scikit-learn, pandas, and ML scoring service
groq>=0.4.0  # For Groq LLM (recommended - fast & free tier available)
scikit-learn>=1.3.0  # For ML models (smart scoring, clustering)
pandas>=2.0.0  # For data processing (doctor_bot pipeline)
joblib>=1.3.0  # For model serialization (doctor_bot pipeline)
googlemaps>=4.10.0  # For Google Places API (legal alternative to scraping)
# openai>=1.0.0  # For OpenAI GPT models
# anthropic>=0.18.0  # For Claude models
# sentence-transformers>=2.2.0  # For embeddings and clustering
# xgboost>=2.0.0  # For ML scoring models (optional)

